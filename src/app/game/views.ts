/*
Views : gs (build) =>
 compute resource growth rates
 
 task speeds (clicked)
 special : level task speed
 task autoclick rate
*/

import * as Types from "@/app/game/types"
import {Item, State, Update} from "@/app/game/types"
import * as Data from "@/app/game/data"

